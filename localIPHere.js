// enter your machines local IP here
// your local IP can be found be writing the following in your terminal:
// ifconfig | grep -Eo 'inet (addr:)?([0-9]*\.){3}[0-9]*' | grep -Eo '([0-9]*\.){3}[0-9]*' | grep -v '127.0.0.1'
// or use google :)

// local IP here
export default "192.168.0.5";
